using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Playables;
public class MainMenuController : MonoBehaviour
{
    [SerializeField] private PlayableAsset mainMenuIdle;
    [SerializeField] private PlayableAsset charSelection;
    [SerializeField] private PlayableAsset charDeSelection;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
